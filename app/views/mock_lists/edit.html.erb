<h1>Editing mock_list</h1>

<% form_for(@mock_list) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @mock_list %> |
<%= link_to 'Back', mock_lists_path %>
