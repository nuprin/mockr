<% @title = "Settings" %>

<%= stylesheet_link_tag "settings/email" %>

<%= render :partial => "/shared/subnav",
           :locals => {
             :header => "Settings",
           } %>

<div id="tabbed_content">
  <% form_for :setting, @setting, :url => setting_path(@setting),
    :html => {:method => :put} do |f| %>
    <%= f.hidden_field :key %>
    <%= f.label :value, "Deliver email notifications to:" %>
    <div class="field_row">
      <%= f.text_field :value %>
      <%= f.submit "Save" %>
    </div>
  <% end %>
</div>
